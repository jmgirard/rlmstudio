% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{lms_server_ready}
\alias{lms_server_ready}
\title{Check whether a host answers as a usable LM Studio server}
\usage{
lms_server_ready(host = "http://localhost:1234", timeout = 2, token = NULL)
}
\arguments{
\item{host}{Character. The base URL of the LM Studio server. Defaults to
"http://localhost:1234".}

\item{timeout}{Numeric. The number of seconds to wait for the request
before giving up. Defaults to 2. A host that opens the port and never
answers reports \code{FALSE} after this many seconds.}

\item{token}{Character or \code{NULL}. An API token for a server that requires
authentication. \code{NULL} reads the \code{rlmstudio.token} option and then the
\code{RLMSTUDIO_API_TOKEN} environment variable. See \link{rlmstudio_token}.}
}
\value{
\code{TRUE} or \code{FALSE}, always one value. This function raises no
condition of its own for a network failure, a refused connection, an
unparsable body, or a failed status. All of those report \code{FALSE}. A
\code{token} that is not one character string and not \code{NULL} still aborts,
because that is a fault in the call rather than a fact about the server.
}
\description{
Sends one GET request to the model list endpoint at \code{host} and reports
whether the answer came from an LM Studio server that this package can use.
The answer is \code{TRUE} only when the request returns HTTP status 200 and the
response body carries a list of models. An empty list counts, because a
fresh LM Studio install has no models downloaded yet and its server still
works.
}
\details{
Use this in place of a port check. Another process holding the port, a
server that has not finished starting, and a server that rejects the token
all answer the port and all report \code{FALSE} here.
}
\examples{
\dontrun{
lms_server_start()

if (lms_server_ready()) {
  list_models()
}

# A server on another port, with a shorter wait
lms_server_ready(host = "http://localhost:8080", timeout = 0.5)
}
}
\seealso{
\code{\link[=lms_server_start]{lms_server_start()}} to start the server. \code{\link[=lms_server_status]{lms_server_status()}}
for what the CLI reports about it.
}
