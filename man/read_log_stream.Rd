% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream.R
\name{read_log_stream}
\alias{read_log_stream}
\title{Read streamed logs from LM Studio}
\usage{
read_log_stream(process)
}
\arguments{
\item{process}{A \code{processx::process} object created by \code{log_stream()}.}
}
\value{
A data frame if the logs were output as JSON and parsed successfully.
Otherwise, returns a character vector of the raw log lines. Returns an empty
data frame if no new logs are available.
}
\description{
Reads the current unread output from a background log stream process created
by \code{log_stream()}. If the stream was started with \code{json = TRUE}, it attempts
to parse the newline-delimited JSON logs into a data frame.
}
\examples{
\dontrun{
stream <- log_stream(source = "server", json = TRUE)

# Wait for some logs to generate, then read them
Sys.sleep(2)
logs_df <- read_log_stream(stream)

stream$kill()
}
}
