% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model_load}
\alias{model_load}
\title{Load a model into memory}
\usage{
model_load(
  model = NULL,
  ttl = NULL,
  gpu = NULL,
  context_length = NULL,
  identifier = NULL,
  estimate_only = FALSE,
  host = NULL
)
}
\arguments{
\item{model}{Character. The model key or path to load. If omitted, the CLI will prompt you.}

\item{ttl}{Integer. Unload the model after this many seconds of inactivity.}

\item{gpu}{Character or Numeric. GPU offload amount. Values can be "max", "off", "auto", or a decimal between 0 and 1.}

\item{context_length}{Integer. The number of tokens to consider as context.}

\item{identifier}{Character. A custom identifier for the loaded model for API reference.}

\item{estimate_only}{Logical. Print a resource (memory) estimate and exit without loading the model. Defaults to FALSE.}

\item{host}{Character. The host address of a remote LM Studio instance.}
}
\value{
Invisibly returns the system exit code (0 for success).
}
\description{
Loads a specified model into LM Studio's memory. You can configure parameters
like context length, GPU offload, and time-to-live (TTL).
}
\examples{
\dontrun{
# Load a model with specific GPU offload and context length
model_load("llama-3.1-8b", gpu = "max", context_length = 4096)

# Estimate memory usage without loading
model_load("llama-3.1-8b", estimate_only = TRUE)
}
}
