% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream.R
\name{log_stream}
\alias{log_stream}
\title{Stream logs from LM Studio}
\usage{
log_stream(
  source = c("model", "server"),
  filter = NULL,
  stats = FALSE,
  json = FALSE
)
}
\arguments{
\item{source}{Character. Source of logs: "model" or "server". Default is "model".}

\item{filter}{Character vector. Filter for model source. Can be "input",
"output", or both (e.g., \code{c("input", "output")}).}

\item{stats}{Logical. Print prediction stats when available.}

\item{json}{Logical. Output logs as newline-separated JSON.}
}
\value{
A \code{processx::process} object representing the background stream.
Use \code{read_log_stream()} to read output from this object.
}
\description{
Spawns a background process to stream logs from LM Studio. This is useful
for debugging prompts sent to the model or inspecting server operations.
}
\examples{
\dontrun{
# Start streaming model IO as JSON
stream <- log_stream(source = "model", json = TRUE)

# Later, stop the background process
stream$kill()
}
}
