% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat.R
\name{lms_chat_batch}
\alias{lms_chat_batch}
\title{Batch Chat Completion with LM Studio}
\usage{
lms_chat_batch(
  model,
  inputs,
  system_prompt = NULL,
  format = c("vector", "list", "data.frame"),
  host = "http://localhost:1234",
  simplify = TRUE,
  quiet = FALSE,
  ...,
  token = NULL
)
}
\arguments{
\item{model}{Character. The loaded model name. Must be one name, given as a
single string.}

\item{inputs}{Character vector. The prompts to process. Must hold at least
one value and no missing values.}

\item{system_prompt}{Character. Optional system prompt.}

\item{format}{Character. Output format: "vector", "list", or "data.frame".}

\item{host}{Character. Server URL.}

\item{simplify}{Logical. If TRUE, parses outputs.}

\item{quiet}{Logical. Whether to suppress the progress bar.}

\item{...}{Additional arguments passed to \code{lms_chat}, such as \code{api_type},
\code{logprobs}, or \code{schema}. A \code{schema} and the \code{api_type} it needs are
checked before the first call.}

\item{token}{Character or \code{NULL}. An API token for a server that requires
authentication. \code{NULL} reads the \code{rlmstudio.token} option and then the
\code{RLMSTUDIO_API_TOKEN} environment variable. See \link{rlmstudio_token}.}
}
\value{
The return type depends on the \code{format} argument:
\itemize{
\item \code{"vector"}: A character vector of responses, with \code{NA} for an input that failed. This format is only supported if \code{simplify = TRUE} and \code{logprobs = FALSE}. With a \code{schema}, it warns and returns the list instead.
\item \code{"list"}: A list where each element is the response corresponding to the provided input, or the condition for an input that failed. With a \code{schema}, \code{simplify = TRUE}, and \code{logprobs = FALSE}, each element that did not fail is the parsed reply.
\item \code{"data.frame"}: A data.frame containing \code{input} and \code{output} columns, with \code{NA} in \code{output} for an input that failed. If \code{logprobs = TRUE}, an additional list-column named \code{logprobs} is included, with \code{NULL} for an input that failed. With a \code{schema} and \code{logprobs = FALSE}, \code{output} is a list-column of parsed replies, with the condition in place of an input that failed.
}
}
\description{
Process a vector of inputs sequentially through LM Studio.
}
\details{
This function calls \code{\link[=lms_chat]{lms_chat()}} once for each element of \code{inputs}. It
raises \code{rlmstudio_no_server} itself, before the first call.

An \code{rlmstudio_api_error} or an \code{rlmstudio_bad_response} that \code{\link[=lms_chat]{lms_chat()}}
raises for one input does not abort the batch. The batch goes on to the
next input. Where the result is a list, or the \code{output} list-column that a
\code{schema} gives, the element for that input holds the condition without its
backtrace. An \code{rlmstudio_bad_response} for a reply that does not parse
keeps the reply content in its \code{content} field. Where the result is text,
the element holds \code{NA}. The result is text with \code{format = "vector"} when it
returns a vector (\code{simplify = TRUE}, no \code{schema}, \code{logprobs = FALSE}), and
with a data frame whose replies are not parsed (no \code{schema}, or
\code{logprobs = TRUE}). The \code{logprobs} column holds \code{NULL} for a failed input.
A reply that \code{\link[=lms_chat]{lms_chat()}} returns as \code{NULL}, such as one whose content is
\code{null}, also holds \code{NA} in a text result. Use \code{format = "list"} to keep the
conditions. The call then gives one warning that names the count
and the positions of the failed inputs. That warning shows even with
\code{quiet = TRUE}.

An \code{rlmstudio_no_server} from \code{\link[=lms_chat]{lms_chat()}} still aborts the batch. Its
\code{results} field holds the results so far, as described in the "Server not
running" section below. An error of any other class aborts the batch
unchanged.
}
\section{Server not running}{

Functions that call the LM Studio REST API open a TCP connection to the
hostname and port named in \code{host} before they send the request. A function
that checks its own arguments does that first, so a bad \code{model}, \code{job_id},
\code{input}, \code{inputs}, or \code{schema} aborts with an argument message and no
condition class
even when the server is down. A condition of class
\code{rlmstudio_no_server} is raised when that connection cannot be opened. A
refused connection raises it. So do an address the package cannot parse and
a hostname that does not resolve. An address that neither accepts nor
refuses the connection also raises it. That case waits for the operating
system to give up, which can take a minute. Start the server with
\code{\link[=lms_server_start]{lms_server_start()}}, or give \code{host} the address that your server listens
on.

The check reads the port and nothing else. Any process holding that port
accepts the connection, so the condition is not raised even though no LM
Studio server is there. The call then fails later, as an
\code{rlmstudio_api_error} or as a raw parse error, rather than as
\code{rlmstudio_no_server}. Use \code{\link[=lms_server_ready]{lms_server_ready()}} for the stronger test: it
asks the host for a model list and reports \code{TRUE} only for an answer that
an LM Studio server would give.

\code{\link[=lms_chat_batch]{lms_chat_batch()}} checks the server once before its first input, and
\code{\link[=lms_chat]{lms_chat()}} checks it again for each input. If that check finds the server
gone during the batch, the batch aborts with \code{rlmstudio_no_server}, and no
request goes out after that. The condition then carries a \code{results} field, a list as
long as \code{inputs}. Its elements before the lost input hold the values that
\code{format = "list"} returns for those inputs. The element of the lost input
and every element after it are \code{NULL}. The check before the first input
adds no \code{results} field. A connection that fails after the check passes,
such as a server that stops during a request, raises an \code{httr2_failure}
error instead. That error aborts the batch and carries no \code{results} field.
}

\section{API failure}{

A condition of class \code{rlmstudio_api_error} is raised when a REST call
returns a response that the wrapper treats as a failure. The condition
carries a \code{status} field, which holds the HTTP response status as an
integer.

\code{\link[=lms_chat_batch]{lms_chat_batch()}} does not abort on it. The element of the failed input
holds the condition, or \code{NA} where the result is text, and the batch warns
once and goes on. See the details of \code{\link[=lms_chat_batch]{lms_chat_batch()}}.
}

\section{Malformed response}{

A condition of class \code{rlmstudio_bad_response} is raised when the server
answers with a status the wrapper accepts and a body the wrapper cannot
read. It is raised where a wrapper checks the body before it reshapes it,
rather than indexing straight into whatever arrived. Two functions raise
it.

\code{\link[=lms_embed]{lms_embed()}} raises it on an embeddings block it cannot trust. The vectors
it returns are placed by the index that the response reports, so a block
with a missing, repeated, or out-of-range index would otherwise pair a
vector with the wrong text and give back a matrix that is silently wrong.

\code{\link[=lms_chat_openai]{lms_chat_openai()}} raises it in two cases, both only with
\code{simplify = TRUE}. The first case is a response whose \code{choices} field is
missing, empty, or not an array, or whose first element is not a JSON
object with a \code{message} object in it, so there is no reply to read. This case is raised with or
without a \code{schema}, and with \code{logprobs = TRUE} as well. The second case is
a reply that does not parse. A \code{schema} was given, \code{logprobs = FALSE}, and
the reply content is not one string of valid JSON. If the server reports
the finish reason \code{"length"}, the token limit cut the reply off. The message
then says so and names \code{max_tokens}. \code{\link[=lms_chat]{lms_chat()}} can raise the condition
through \code{\link[=lms_chat_openai]{lms_chat_openai()}}.

\code{\link[=lms_chat_batch]{lms_chat_batch()}} does not abort on it. The element of the failed input
holds the condition, or \code{NA} where the result is text, and the batch warns
once and goes on. See the details of \code{\link[=lms_chat_batch]{lms_chat_batch()}}.

The condition carries a \code{status} field, which holds the HTTP response
status as an integer. Today the status is always 200: both functions read
the body only after a 200, and report every other status as an
\code{rlmstudio_api_error} instead. A condition from \code{\link[=lms_chat_openai]{lms_chat_openai()}} also
carries two more fields. The \code{content} field holds the reply content, and
the \code{finish_reason} field holds the finish reason that the server reported.
Either one is \code{NULL} where the response has none, and both are \code{NULL} for a
response with no \code{choices}. For a reply that does not parse, the message
names the \code{content} field, so you can read what the model wrote without a
second request. The other messages name \code{simplify = FALSE}, which returns
the body unchanged, with one exception. An embeddings body that did not
parse at all is checked before that argument is read, so its message
points at the host instead.
}

