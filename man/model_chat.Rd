% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat.R
\name{model_chat}
\alias{model_chat}
\title{Chat with a local model}
\usage{
model_chat(
  model = NULL,
  prompt = NULL,
  system_prompt = NULL,
  stats = FALSE,
  ttl = NULL,
  capture = FALSE
)
}
\arguments{
\item{model}{Character. Identifier of the model to use. If omitted, the CLI will prompt you.}

\item{prompt}{Character. Send a one-off prompt and exit without staying interactive.}

\item{system_prompt}{Character. Custom system prompt for the chat.}

\item{stats}{Logical. Show detailed prediction statistics after each response. Defaults to FALSE.}

\item{ttl}{Integer. Seconds to keep the model loaded after the chat ends.}

\item{capture}{Logical. If \code{TRUE} and a \code{prompt} is provided, the function will
return the response as a character vector instead of printing it to the console.}
}
\value{
If \code{capture = TRUE} and a \code{prompt} is provided, returns a character
vector of the model's response. Otherwise, invisibly returns the system exit code.
}
\description{
Starts an interactive chat session with a local model in the terminal, or
sends a single prompt and returns the response.
}
\examples{
\dontrun{
# Start an interactive chat
model_chat("llama-3.1-8b")

# Send a one-off prompt and capture the text in R
response <- model_chat("llama-3.1-8b", prompt = "What is 2+2?", capture = TRUE)
}
}
