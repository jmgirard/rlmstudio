% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model_get}
\alias{model_get}
\title{Search and download models}
\usage{
model_get(
  model_name = NULL,
  mlx = FALSE,
  gguf = FALSE,
  limit = NULL,
  always_show_all_results = FALSE,
  always_show_download_options = FALSE
)
}
\arguments{
\item{model_name}{Character. The model to search for or download. For models
with multiple quantizations, append '@' (e.g., "llama-3.1-8b@q4_k_m").}

\item{mlx}{Logical. Include only MLX models in search results.}

\item{gguf}{Logical. Include only GGUF models in search results.}

\item{limit}{Integer. Limit the number of model options shown in search results.}

\item{always_show_all_results}{Logical. Always prompt you to choose from search
results, even when there is an exact match.}

\item{always_show_download_options}{Logical. Always prompt you to choose a
quantization, even when an exact match is auto-selected.}
}
\value{
Invisibly returns the system exit code (0 for success).
}
\description{
Searches for and downloads models from online repositories via the LM Studio CLI.
If no model name is specified, it shows staff-picked recommendations.
}
\details{
This function is interactive. It will print search results and download
progress directly to your R console. If multiple matches are found, it may
prompt you to choose one.
}
\examples{
\dontrun{
# Show staff-picked recommendations
model_get()

# Search for a specific model
model_get("llama-3.1-8b")

# Download an exact quantization and limit search results
model_get("llama-3.1-8b@q4_k_m", limit = 5)
}
}
