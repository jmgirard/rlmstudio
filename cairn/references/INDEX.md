# References index

One line per committed page under `cairn/references/`. The `sources/` shelf is gitignored.

- `lmstudio-api-surface.md` — endpoint-by-endpoint coverage of the LM Studio REST API and the `lms` CLI against the package, read 2026-09-19.
