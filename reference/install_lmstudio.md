# Help the user install or update LM Studio

Help the user install or update LM Studio

## Usage

``` r
install_lmstudio(method = c("browser", "headless"))
```

## Arguments

- method:

  Character. Either "browser" or "headless".
