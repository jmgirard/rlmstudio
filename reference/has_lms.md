# Check if LM Studio CLI is installed

Check if LM Studio CLI is installed

## Usage

``` r
has_lms()
```

## Value

Logical. TRUE if lms is found, FALSE otherwise.

## Examples

``` r
if (FALSE) { # \dontrun{
has_lms()
} # }
```
